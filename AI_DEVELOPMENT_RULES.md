# AI Development Rules

本文件是项目开发时给 AI 助手读取并严格执行的统一规范。每次开始新项目或新任务前，AI 必须先阅读本文件，并在执行过程中持续遵守。

## 0. 使用方式

在每个新项目开始时，把本文件放到项目根目录，推荐命名为：

- `AI_DEVELOPMENT_RULES.md`
- `CLAUDE.md`
- `AGENTS.md`
- `.cursor/rules/project.md`

首次对话时使用以下提示：

```text
请先阅读项目根目录下的 AI_DEVELOPMENT_RULES.md，并在本次开发全过程中严格执行。除非我明确要求变更规则，否则不要跳过、弱化或自行改写这些约束。
```

## 0.1 质量规则（强制执行）

**执行任何 skill 前，必须先读取并执行 `QUALITY_RULES.md` 的规则。**

项目根目录下的 `QUALITY_RULES.md` 是质量增强规则，补充本文件以下维度：

- 各阶段产出前的自检清单（需求/选型/架构/实现）— 内部自检，不问用户
- 静态检查强制规则（Python: black/isort/mypy/pylint/bandit，TypeScript: prettier/eslint/tsc/npm audit）
- 代码质量标准（类型标注/错误处理/函数与模块/命名规范）
- 测试质量标准（覆盖率/组织/命名）
- 自动确认规则（什么时候 Agent 可以自主推进，什么时候必须停下确认）
- 追溯矩阵维护规则
- 失败处理规则与重试策略
- 评审规则自优化元规则

**执行优先级**：
1. 先读 `AI_DEVELOPMENT_RULES.md`（本文件）— 通用行为准则
2. 再读 `QUALITY_RULES.md` — 质量增强规则
3. 冲突时以本文件为准，QUALITY_RULES.md 只做增强
4. 两份文件都必须读取，不得跳过

**特别强调**：feature / architect / tech / integration-test / review / scaffold / traceability / fix / change 这些 skill 启动时，必须读取 QUALITY_RULES.md 并在执行过程中遵循其规则。

## 1. 核心原则

1. 先理解，再修改。
2. 优先遵循现有项目结构、技术栈、命名风格和代码习惯。
3. 不做与当前任务无关的重构。
4. 不臆造不存在的接口、文件、字段、依赖或业务规则。
5. 遇到不确定的信息，先从代码、配置、文档、测试中查证。
6. 修改必须小步、可验证、可回退。
7. 完成后必须说明改了什么、为什么改、如何验证。

## 2. 开发前必须执行

开始写代码前，AI 必须完成以下检查：

1. 阅读项目结构，识别主要技术栈、入口文件、构建方式和测试方式。
2. 查找与当前需求相关的文件、组件、接口、状态管理、数据库模型或配置。
3. 如果已有相似功能，优先复用现有模式。
4. 如果需求不清楚，先提出最少数量的关键问题。
5. 如果可以根据上下文合理判断，则直接执行，不要频繁打断用户。

禁止：

- 不看代码直接给方案。
- 用全新架构覆盖已有架构。
- 为了实现小功能引入大型依赖。
- 修改无关文件。
- 删除用户已有改动。

## 3. 修改代码规则

AI 修改代码时必须遵守：

1. 保持改动范围最小。
2. 保持文件职责清晰，不把无关逻辑塞进同一个文件。
3. 优先使用项目已有工具函数、组件、样式变量、请求封装和错误处理方式。
4. 新增代码必须可读、可维护，避免炫技。
5. 不写无意义注释，只在复杂逻辑前添加简短说明。
6. 不硬编码敏感信息、密钥、账号、真实 token、生产地址。
7. 不吞掉错误，必须保留合理的错误处理和用户反馈。
8. 不为了让测试通过而降低业务正确性。

## 4. 前端开发规则

如果任务涉及前端，必须遵守：

1. 先读取 `docs/product/15-frontend-design.md` 和 `docs/product/15-frontend-design-tokens.json`；如果缺失或内容为空，必须先根据项目类型补齐 UI 风格参考、设计关键词和 token 值。
2. 所有颜色、间距、圆角、字体、阴影必须引用 `15-frontend-design-tokens.json` 中的 token 值，禁止硬编码随意值。如果需要新增 token，先添加到 token 文件再引用。
3. 优先匹配现有设计系统、组件库、间距、字体、颜色和交互习惯。
3. 页面必须在桌面端和移动端都不出现明显布局错乱。
4. 文本不能溢出按钮、卡片、表格单元格或弹窗。
5. 表单必须有明确的校验、错误提示和提交状态。
6. 异步数据必须处理 loading、empty、error、success 状态。
7. 交互控件要符合用户直觉：开关用 switch/checkbox，选项用 select/tabs/segmented control，危险操作要二次确认。
8. 不做空洞的营销页，除非需求明确要求。
9. 不添加与业务无关的装饰性元素。
10. 产品形态、UI 风格、布局、组件策略或交互状态发生变化时，必须反向更新 `docs/product/15-frontend-design.md`。

## 5. 后端开发规则

如果任务涉及后端，必须遵守：

1. API 入参必须校验。
2. 权限、身份、租户隔离等安全边界必须显式处理。
3. 数据库写入必须考虑事务、一致性和失败回滚。
4. 错误返回必须稳定、可诊断，不暴露敏感信息。
5. 日志必须有助于排查问题，但不能记录密码、token、身份证、银行卡等敏感信息。
6. 新增接口必须保持响应结构与项目既有风格一致。
7. 涉及分页、排序、搜索时，要明确默认值和边界条件。

## 6. 测试与验证规则

### 6.1 测试先行（TDD，项目有测试框架时默认启用）

如果项目已配置测试框架（jest/vitest/pytest/go test/junit 等），实现新功能或修复 bug 时必须遵循：

1. 先写失败测试（Red）：根据需求或 bug 编写最少验证用例，运行确认失败。
2. 最小实现（Green）：写刚好让测试通过的代码，不提前优化。
3. 重构（Refactor）：在测试保护下优化命名、抽象、性能，确保测试仍然通过。
4. 确认所有相关测试通过后，再进入下一步。

如果项目没有测试框架，跳过本步骤但在最终回复中标注"本项目未配置测试框架，建议后续补充"。

如果是纯 UI 调整、文档变更或配置修改等不适合单测的场景，跳过 TDD 但必须在验证门禁中提供替代证据。

### 6.2 验证门禁

实现完成后，AI 必须提供以下证据（至少满足 2 项）才能标记任务完成：

- [ ] 测试通过输出（单测、集成、E2E 任一）
- [ ] 构建成功输出（build/compile 无 error）
- [ ] 类型检查通过（tsc/mypy/go vet 等）
- [ ] 接口响应示例（curl/httpie/浏览器 devtools）
- [ ] 页面手动测试步骤（见 §9.1）

无法提供证据时，必须说明原因和剩余风险，不能默认标记完成。

### 6.3 基本验证流程

完成修改后，AI 必须尽可能执行验证：

1. 优先运行与改动相关的最小测试。
2. 如果有 lint/typecheck/build/test 命令，应根据改动风险选择运行。
3. 前端视觉改动需要启动页面并检查实际效果。
4. 修 bug 时必须说明复现原因和验证方式。
5. 如果无法运行测试，必须明确说明原因和剩余风险。

推荐验证顺序：

```text
相关单测 -> 类型检查 -> lint -> build -> 手动功能验证
```

## 7. Git 与文件安全规则

AI 必须遵守：

1. 不执行 `git reset --hard`。
2. 不执行会丢失用户改动的 checkout/revert 操作，除非用户明确要求。
3. 不删除不理解的文件。
4. 不改动锁文件，除非依赖确实发生变化。
5. 不提交代码，除非用户明确要求。
6. 遇到工作区已有改动时，默认认为是用户改动，必须保留。

## 8. 依赖与配置规则

1. 新增依赖前必须说明理由。
2. 能用项目已有依赖解决的问题，不新增依赖。
3. 不随意升级框架、构建工具、语言版本。
4. 配置改动必须说明影响范围。
5. 环境变量必须提供示例，不提交真实密钥。

## 8.0 技术选型确认规则

当任务涉及以下内容时，AI 不能直接决定技术方案，必须先给出方案推荐并等待用户确认：

1. 前端框架、后端框架、构建工具、运行时。
2. 数据库、缓存、消息队列、搜索引擎、对象存储。
3. 鉴权、权限、支付、邮件、短信、推送、第三方登录。
4. 部署平台、CI/CD、容器化、云服务、监控、日志、任务调度。
5. 图表、富文本、地图、表格、文件预览等重型前端依赖。
6. 会影响架构、性能、成本、安全、维护方式的新增依赖或服务。

技术选型流程必须是：

1. 说明当前技术需求和约束。
2. 给出 2-3 个可选方案。
3. 对比每个方案的目的、优点、缺点、性能影响、成本和维护复杂度。
4. 给出推荐方案和推荐理由。
5. 等待用户确认。
6. 确认后写入 `docs/engineering/04-tech-decisions.md`。
7. 再进入实现。

除非用户明确指定某个技术，并明确表示不需要比较方案，否则 AI 不能跳过本规则。

## 8.1 性能设计规则

AI 在设计方案和实现新功能时必须提前考虑性能：

1. 前端优先考虑按需引入、懒加载、代码分割和资源体积控制。
2. 大型组件、图表、编辑器、地图、富文本等默认不进入首屏主包，除非项目明确需要。
3. 列表、表格、日志、消息流等大数据场景必须考虑分页、虚拟滚动或增量加载。
4. 请求层必须避免重复请求、瀑布请求和过大的响应体。
5. 后端必须考虑查询索引、分页、缓存、批量处理、连接复用、超时和重试边界。
6. 新增依赖必须说明性能影响和是否支持按需引入。
7. 如果为了简单暂时不做性能优化，必须说明原因和后续补点。

## 9. 文档输出规则

当任务涉及文档或交付说明时，AI 应输出：

1. 修改了哪些文件。
2. 核心实现思路。
3. 运行或验证命令。
4. 已知限制或后续建议。

回答要直接、具体，不要堆砌空话。

## 9.1 页面手动测试步骤输出规则

完成一个需求、功能、变更或 bug 修复后，如果涉及用户可见页面、交互流程、接口数据在页面上的表现，AI 在最终回复中必须给出“页面手动测试步骤”。

测试步骤必须能让用户直接在浏览器页面中验证，不只是写命令。

输出应包含：

1. 进入哪个页面或地址。
2. 使用什么账号、角色或前置数据。
3. 点击哪些按钮或执行哪些操作。
4. 输入哪些测试数据。
5. 预期看到什么页面变化、提示、数据结果或错误状态。
6. 至少包含一个正常流程。
7. 如果功能有明显边界，补充 1-2 个异常/边界流程。

如果本次改动不涉及页面，例如纯后端、脚本、配置或文档，AI 必须说明“本次无页面手测步骤”，并给出可替代的验证方式。

## 10. 沟通规则

AI 与用户沟通时必须：

1. 先给结论，再给必要解释。
2. 不确定时明确说明不确定点。
3. 不假装已经验证未验证的内容。
4. 不把猜测写成事实。
5. 遇到多种方案时，给出推荐方案和取舍理由。
6. 用户要求直接实现时，不只停留在方案层。

## 11. 任务完成标准

一个开发任务只有在满足以下条件后才算完成：

1. 需求中的主要功能已经实现。
2. 相关错误和边界情况已处理。
3. 已运行必要验证，或明确说明无法验证的原因。
4. 没有引入明显无关改动。
5. 最终回复包含清晰的完成说明。

## 12. AI 必须主动避免的问题

以下行为默认禁止：

1. 大段重写用户没有要求修改的模块。
2. 用 mock 数据掩盖真实接口问题。
3. 删除测试来让结果通过。
4. 用 `any`、空 catch、硬编码超时等方式绕过类型或错误。
5. 将复杂业务逻辑写进 UI 组件而不拆分。
6. 把权限校验只放在前端。
7. 只看文件名猜业务含义。
8. 生成无法运行的伪代码后声称已完成。

## 13. 可选项目定制区

以下内容可以按项目补充。

### 技术栈

```text
前端：
后端：
数据库：
部署：
测试：
```

### 代码风格

```text
命名规则：
目录规则：
组件规则：
接口规则：
错误处理规则：
```

### 业务红线

```text
权限规则：
数据隔离规则：
支付/订单规则：
审计/日志规则：
合规要求：
```

### 验收标准

```text
功能验收：
性能验收：
兼容性验收：
安全验收：
```

## 14. 推荐首次对话模板

```text
你现在是本项目的开发助手。

请先阅读并遵守项目根目录的 AI_DEVELOPMENT_RULES.md。

接下来你需要完成的任务是：
【在这里写具体需求】

要求：
1. 先阅读相关代码和项目结构。
2. 不要修改无关文件。
3. 实现后运行必要验证。
4. 最后说明修改内容、验证结果和剩余风险。
```

## 16. 本项目跨项目规则

本节从 `retro/assets/rules/` 中按当前项目实际情况选择并裁剪。当前复用 7 条规则；
Hibernate 实体规则与已确认的 Python/SQLAlchemy 技术栈不适配，图谱存储规则因当前需求不包含 GraphRAG 暂不纳入。

### 16.1 产品应用定位边界

本项目定位为面向公司内部终端用户的产品应用，不同时承担面向其他系统或 Agent 的基础设施服务职责。

- 主要入口是 Web 用户端和管理后台。
- 身份边界是统一本地账号、角色和系统负责人映射，知识数据以业务系统为隔离单元。
- 首版区分用户端与管理端登录入口，但共用账号表和认证服务；用户由受控 SQL/导入脚本批量创建，管理员由后台创建，并预留公司 SSO 适配接口。
- 如果未来需要把检索或 Agent 能力作为通用平台 API 对外提供，应另行立项和设计服务间鉴权、租户模型及兼容性边界。

### 16.2 可复用资产盘点时机

完成多系统问答、内置工单、答案回流的核心闭环，并完成 1-2 个质量增强阶段后，执行一次跨项目资产盘点。
重点识别文档解析、引用模型、检索降级、提示词版本、通知适配和工单回流中与业务无关的可复用模块。

### 16.3 账号变更后的会话失效

本项目采用 Redis 服务端 Session。账号禁用、角色或系统负责人映射变更、密码重置和首次改密完成后，必须撤销受影响的旧 Session；
Redis 不可用时鉴权失败关闭，不得为了可用性默认放行。批量导入的默认密码只保存 Argon2id 摘要，并强制首次改密。

### 16.4 RAG 多通道降级

检索与生成链路必须显式降级，任何可选通道失败都不能拖垮整个问答流程：

1. Rerank 不可用时降级到基础召回排序。
2. 向量检索不可用时降级到关键词检索。
3. 生成模型不可用或证据不足时停止生成，返回可诊断状态并进入工单流程。
4. 降级过程必须记录日志和指标，且不得跨业务系统检索或泄露知识。

### 16.5 LLM 提示词版本化

意图识别、问答、拒答判断、摘要等提示词必须独立于业务代码保存，包含版本、启用状态、创建时间和变更说明，
支持效果对比与回滚。不得把可运营提示词硬编码在 service 或 UI 代码中。

### 16.6 外部服务和持久化必须有集成测试

LLM、Embedding、Rerank、通知 API、PostgreSQL、Redis、对象存储和异步任务不能只通过 mock 验证。
公司环境不允许 Docker，因此不强制 Testcontainers；应使用隔离测试库/Schema、测试 Bucket 和测试服务端点，
并在类生产 Linux 环境跑通至少一条真实核心链路。

### 16.7 可调参数从第一天配置化

模型名称、批大小、检索 top-k、相似度阈值、Rerank top-k、文件大小限制、超时、重试、线程池、队列容量和提示词版本
必须从配置或受控配置表读取。密钥只通过环境变量或公司密钥管理设施提供，不进入仓库。

### 16.8 apply_patch 失败即停与行号锚定整段替换（修复阶段工具切换规则）

**适用场景**：在 review 修复或任何多文件、多修复点改动中使用 Codex `apply_patch` 时。跨项目通用，前端 TSX/Vue 等缩进深、字面相似块多的文件尤其高发。

**规则内容**：

1. **`apply_patch` 第一次失败即停**：无论形式是「Failed to find expected lines」报错，还是「补丁应用成功但语义不对」的静默半成功（典型表现：改了类型声明 `{ form?, crossFieldValidator? }` 却没动对应的解构赋值 `{ name, label, step = 1 }`，留下 `form` 在运行时为 `undefined` 的半正确文件），都立即停止在同一 hunk 上重试。不在同一处尝试第二次 patch——反复重试是修复阶段最大的单点时间黑洞，实测可累积 50 分钟以上。

2. **切换的默认动作是「行号锚定整段替换」，不是「构造更大的 patch」**：先用 `sed -n '<起始行>,<结束行>p' <file>` 重新锚定当前真实状态，确认落点无误后，用 Python `lines[a:b] = [<新段>]`（0-indexed 切片）做整段替换。`apply_patch` 的内容匹配对空白字符和跨行上下文脆弱，TSX 深缩进里尤其易触发；行号锚定彻底绕开这个脆性，且 `sed -n` 校验落点能消除「匹配到错的那一处」的不确定性。

3. **存在字面相似但语义不同的代码块时，直接整段行号重写，不靠 `apply_patch` 内容匹配**：一个组件里有两处 `{ form }` / 两处 `{ name, label }`、类型声明与解构赋值分处两段、或者两处相似 import——这类场景是 `apply_patch` 静默半成功的高发区。识别到这类结构后，跳过 patch 直接用行号替换，并在替换后立即 `grep -n` 确认所有相似块都被正确覆盖。

4. **每完成一个修复点立即跑对应静态检查，不攒到收尾**：后端改完跑 `mypy --strict <file>`，前端改完跑 `tsc --noEmit` 或 `vue-tsc --noEmit`。这条的作用是让「半正确文件」在用户意识到之前就被 tsc/mypy 揪出来——`apply_patch` 的静默半成功只靠静态检查才能探测，攒到最后批量跑会让错误堆叠并互相干扰定位。验证通过后再进入下一个修复点。

**与 §16.7 的关系**：§16.7 关注「可调参数从配置化而非硬编码」，是架构纪律层面；本规则关注「`apply_patch` 在修改既有文件时内容匹配脆弱，何时该停手并切换为行号锚定整段替换」，是反馈回路层面的工具切换策略。两者互补——§16.7 让代码从第一天就配置化，本规则让修复阶段的工具使用快速终止失败并切换。

**来源**：Phase 14 review 修复（2026-08-07）复盘归因。本次近 2 小时修复中，`apply_patch` 在多处后端 Python 文件反复失败（每次因函数间空行数量、缩进等微差异报错），单点消耗 8 分钟；横向复盘 ConfigurationPage.tsx 类前端场景，静默半成功可累积 50+ 分钟黑洞。根因不是「工具难用」或「mypy 难」，而是反复重试同一脆性工具且未在修复点粒度跑静态检查，让静默半成功堆积。写入规则避免后续重复踩坑。
